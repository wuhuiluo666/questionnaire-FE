import { ComponentTitle, defaultProps } from '.'
import { TitleProps } from './titleProps'

export const ComponentTitleConfig = {
  title: '标题',
  type: 'questionTitle',
  Component: ComponentTitle,
  ComponentProps: TitleProps,
  defaultProps: defaultProps
}

