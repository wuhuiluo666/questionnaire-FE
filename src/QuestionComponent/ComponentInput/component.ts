import { ComponentInput, defaultProps } from '.'
import { InputProps } from './inputProps'
export const ComponentInputConfig = {
  title: '输入框',
  type: 'questionInput',
  Component: ComponentInput,
  ComponentProps: InputProps,
  defaultProps: defaultProps
}
