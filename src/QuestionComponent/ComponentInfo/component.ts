import { ComponentInfo, defaultProps } from '.'
import { InfoProps } from './infoProps'

export const ComponentInfoConfig = {
  title: '问卷标题',
  type: 'questionInfo',
  Component: ComponentInfo,
  ComponentProps: InfoProps,
  defaultProps: defaultProps
}
